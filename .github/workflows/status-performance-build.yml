name: Status de Performance - Build & Release

on:
  push:
    tags:
      - 'status-performance-v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        working-directory: "Status de Performance"
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        working-directory: "Status de Performance"
        run: |
          pyinstaller --onefile --noconsole --name "Status de Performance" --icon icon.ico main.py 2>$null; if (!(Test-Path "dist/Status de Performance.exe")) { pyinstaller --onefile --noconsole --name "Status de Performance" main.py }

      - name: Create ZIP artifact
        working-directory: "Status de Performance"
        run: |
          Compress-Archive -Path "dist/Status de Performance.exe" -DestinationPath "dist/StatusPerformance-${{ github.ref_name }}.zip"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Status de Performance ${{ github.ref_name }}"
          body: |
            ## Status de Performance ${{ github.ref_name }}

            Lightweight system performance monitoring overlay.

            ### Download
            - **Windows**: `.zip` with portable `.exe`

            ### Features
            - CPU, GPU, RAM and temperature monitoring
            - Transparent always-on-top overlay
            - Configurable hotkeys
            - Drag mode for positioning
            - System tray integration
          files: |
            Status de Performance/dist/StatusPerformance-${{ github.ref_name }}.zip
            Status de Performance/dist/Status de Performance.exe
